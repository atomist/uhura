query KubernetesClusterProviderForName($name: String) {
    KubernetesClusterProvider(name: $name) {
        id
        name
        url
        state {
            name
            error
        }
    }
}
